/*
 * Archivo de prueba para las dependencias.
 * Primer caso simple donde se hace referencia a un
 * solo archivo dentro del mismo directorio.
 * Se agrega el uso de LOCATION
 */
use dir2.dummyX;
location dir2;